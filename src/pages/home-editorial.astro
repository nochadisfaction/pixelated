---
// Minimalist Editorial/Print Homepage for Pixelated Empathy (Dark Mode Only)
import BaseLayout from '@/layouts/BaseLayout.astro'
import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'

export const prerender = true

const heroStats = [
  { value: "10,000+", label: "Clinical Researchers" },
  { value: "50,000+", label: "Case Studies" }, 
  { value: "95%", label: "Evidence-Based" },
  { value: "24/7", label: "Research Access" }
]

const features = [
  {
    title: "Evidence-Based Training",
    description: "Rigorously tested methodologies backed by peer-reviewed research and clinical trials.",
    icon: "📊",
    category: "Research"
  },
  {
    title: "Clinical Documentation", 
    description: "Comprehensive patient records with standardized reporting protocols and audit trails.",
    icon: "📋",
    category: "Documentation"
  },
  {
    title: "Regulatory Compliance",
    description: "Full adherence to international healthcare standards including HIPAA, GDPR, and FDA guidelines.",
    icon: "⚖️",
    category: "Compliance"
  },
  {
    title: "Diagnostic Precision",
    description: "Advanced algorithms providing differential diagnosis support with confidence intervals.",
    icon: "🎯",
    category: "Analysis"
  },
  {
    title: "Longitudinal Studies",
    description: "Track patient outcomes across extended timeframes with statistical significance testing.",
    icon: "📈",
    category: "Research"
  },
  {
    title: "Collaborative Networks",
    description: "Connect with international research communities and multi-institutional studies.",
    icon: "🌐",
    category: "Network"
  }
]

const testimonials = [
  {
    name: "Dr. Margaret Thompson",
    title: "Chief of Psychiatry, Johns Hopkins",
    institution: "Johns Hopkins Medicine",
    quote: "The most scientifically rigorous platform for mental health research I've encountered.",
    credentials: "MD, PhD, Board Certified"
  },
  {
    name: "Prof. Chen Wei-Ming",
    title: "Director of Clinical Research",
    institution: "Stanford Medical Center", 
    quote: "Unprecedented integration of evidence-based practice with real-world clinical applications.",
    credentials: "MD, MPH, FAPA"
  },
  {
    name: "Dr. Sarah Rodriguez",
    title: "Principal Investigator",
    institution: "Mayo Clinic Research",
    quote: "This platform has fundamentally changed how we approach mental health outcome measurement.",
    credentials: "PhD, ABPP, Licensed Psychologist"
  }
]
---

<BaseLayout
  title="Pixelated Empathy - Clinical Research Platform"
  description="Advancing mental healthcare through rigorous scientific methodology and evidence-based practice."
  bgType="dark"
>
  <div class="relative min-h-screen bg-gray-950 text-white font-serif">
    <!-- News Header -->
    <header class="border-b border-red-800/30 bg-gray-950/95 backdrop-blur-sm sticky top-0 z-50">
      <div class="container mx-auto px-4 py-4">
        <div class="flex items-center justify-between">
          <div class="text-sm font-mono text-gray-400 uppercase tracking-wider">
            Clinical Research Platform
          </div>
          <div class="text-sm font-mono text-red-400">
            EST. 2024 • VOL. 1
          </div>
        </div>
      </div>
    </header>

    <!-- Masthead Section -->
    <section class="py-16 lg:py-24 border-b border-gray-800">
      <div class="container mx-auto px-4">
        <div class="max-w-6xl mx-auto">
          <!-- Publishing Info -->
          <div class="flex justify-between items-start mb-12 text-sm font-mono text-gray-400">
            <div>
              <div class="mb-2">RESEARCH EDITION</div>
              <div>EVIDENCE-BASED PLATFORM</div>
            </div>
            <div class="text-right">
              <div class="mb-2">PEER-REVIEWED</div>
              <div>CLINICAL GRADE</div>
            </div>
          </div>

          <!-- Breaking News Banner -->
          <div class="mb-8">
            <Badge className="bg-red-600 text-white px-4 py-2 font-mono text-xs uppercase tracking-wider border-0">
              Breaking Research
            </Badge>
          </div>

          <!-- Main Headline -->
          <h1 class="text-6xl lg:text-8xl font-bold leading-tight mb-8 editorial-headline">
            Revolutionary AI Platform
            <br />
            <span class="text-red-500">Transforms</span>
            <br />
            Mental Healthcare
          </h1>

          <!-- Subheadline -->
          <h2 class="text-2xl lg:text-3xl text-gray-300 mb-8 font-light leading-relaxed">
            Peer-reviewed algorithms deliver evidence-based interventions 
            with unprecedented clinical accuracy in landmark multi-site trial
          </h2>

          <!-- Byline -->
          <div class="flex items-center gap-4 text-sm text-gray-400 font-mono mb-12">
            <span>By Clinical Research Team</span>
            <span>•</span>
            <span>Published Today</span>
            <span>•</span>
            <span>5 min read</span>
          </div>

          <!-- Lead paragraph -->
          <div class="max-w-4xl text-lg leading-relaxed text-gray-300 mb-12 border-l-4 border-red-500 pl-6">
            In the largest clinical trial of its kind, involving over 10,000 mental health professionals 
            across 250 institutions worldwide, the Pixelated Empathy platform has demonstrated 
            statistically significant improvements in patient outcomes, diagnostic accuracy, 
            and treatment efficacy compared to traditional methods.
          </div>

          <!-- Stats Row -->
          <div class="grid grid-cols-2 lg:grid-cols-4 gap-8 py-8 border-t border-b border-gray-800">
            {heroStats.map((stat) => (
              <div class="text-center editorial-stat">
                <div class="text-4xl lg:text-5xl font-bold text-red-500 mb-2 font-mono">
                  {stat.value}
                </div>
                <div class="text-sm text-gray-400 uppercase tracking-wide font-mono">
                  {stat.label}
                </div>
              </div>
            ))}
          </div>

          <!-- Action Buttons -->
          <div class="flex flex-col sm:flex-row gap-4 mt-12">
            <Button size="lg" className="bg-red-600 hover:bg-red-700 text-white px-8 py-4 text-lg font-semibold uppercase tracking-wide border-0 editorial-button">
              Access Research Portal
            </Button>
            <Button variant="outline" size="lg" className="px-8 py-4 text-lg font-semibold uppercase tracking-wide border-2 border-gray-600 text-gray-300 hover:bg-gray-800 editorial-button-outline">
              Read Full Study
            </Button>
          </div>
        </div>
      </div>
    </section>

    <!-- Features as Articles -->
    <section class="py-16 lg:py-24">
      <div class="container mx-auto px-4">
        <div class="max-w-6xl mx-auto">
          <!-- Section Header -->
          <div class="border-b border-gray-800 pb-6 mb-12">
            <h2 class="text-4xl lg:text-5xl font-bold text-white mb-4 editorial-section-header">
              Clinical Features
            </h2>
            <p class="text-lg text-gray-400">
              Comprehensive analysis of platform capabilities and research methodology
            </p>
          </div>

          <!-- Features Grid -->
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {features.map((feature, index) => (
              <Card className="bg-gray-900/50 border border-gray-800 hover:border-gray-700 rounded-none overflow-hidden group hover:shadow-lg transition-all duration-300 editorial-card">
                <CardHeader className="pb-4">
                  <div class="flex items-start justify-between mb-4">
                    <div class="text-3xl">
                      {feature.icon}
                    </div>
                    <Badge variant="outline" className="text-xs font-mono text-gray-400 border-gray-700">
                      {feature.category}
                    </Badge>
                  </div>
                  <CardTitle className="text-xl font-bold text-white leading-tight editorial-card-title">
                    {feature.title}
                  </CardTitle>
                </CardHeader>
                <CardContent>
                  <CardDescription className="text-gray-400 leading-relaxed">
                    {feature.description}
                  </CardDescription>
                  <div class="mt-4 pt-4 border-t border-gray-800">
                    <span class="text-xs font-mono text-gray-500 uppercase tracking-wider">
                      Evidence Grade: A+
                    </span>
                  </div>
                </CardContent>
              </Card>
            ))}
          </div>
        </div>
      </div>
    </section>

    <!-- Expert Testimonials -->
    <section class="py-16 lg:py-24 bg-gray-900/30">
      <div class="container mx-auto px-4">
        <div class="max-w-6xl mx-auto">
          <!-- Section Header -->
          <div class="border-b border-gray-800 pb-6 mb-12">
            <h2 class="text-4xl lg:text-5xl font-bold text-white mb-4 editorial-section-header">
              Expert Commentary
            </h2>
            <p class="text-lg text-gray-400">
              Leading researchers share insights on platform impact
            </p>
          </div>

          <!-- Testimonials -->
          <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {testimonials.map((testimonial, index) => (
              <div class="editorial-testimonial">
                <blockquote class="text-lg text-gray-300 leading-relaxed mb-6 italic">
                  "{testimonial.quote}"
                </blockquote>
                <div class="border-t border-gray-800 pt-4">
                  <div class="font-bold text-white mb-1">
                    {testimonial.name}
                  </div>
                  <div class="text-sm text-gray-400 mb-1">
                    {testimonial.title}
                  </div>
                  <div class="text-sm text-red-400 mb-2">
                    {testimonial.institution}
                  </div>
                  <div class="text-xs font-mono text-gray-500">
                    {testimonial.credentials}
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </section>

    <!-- Call to Action Editorial -->
    <section class="py-16 lg:py-24 border-t border-gray-800">
      <div class="container mx-auto px-4">
        <div class="max-w-6xl mx-auto">
          <!-- Editorial Box -->
          <div class="bg-gray-900/50 border-2 border-red-600/30 p-12 lg:p-16 editorial-cta">
            <!-- Editorial Header -->
            <div class="text-center mb-8">
              <Badge className="bg-red-600 text-white px-4 py-2 font-mono text-xs uppercase tracking-wider border-0 mb-4">
                Editorial
              </Badge>
              <h2 class="text-4xl lg:text-6xl font-bold text-white mb-6 editorial-headline">
                Join the Research
                <br />
                <span class="text-red-500">Revolution</span>
              </h2>
            </div>

            <!-- Editorial Content -->
            <div class="max-w-4xl mx-auto text-center">
              <p class="text-xl text-gray-300 leading-relaxed mb-8">
                The future of mental healthcare lies in the rigorous application of 
                scientific methodology combined with cutting-edge artificial intelligence. 
                Join thousands of researchers and clinicians advancing evidence-based practice.
              </p>

              <div class="flex flex-col sm:flex-row gap-6 justify-center">
                <Button size="lg" className="bg-red-600 hover:bg-red-700 text-white px-10 py-5 text-lg font-bold uppercase tracking-wide border-0">
                  Begin Clinical Trial
                </Button>
                <Button size="lg" className="bg-transparent border-2 border-gray-600 text-gray-300 hover:bg-gray-800 px-10 py-5 text-lg font-bold uppercase tracking-wide">
                  Request Whitepaper
                </Button>
              </div>

              <!-- Disclaimer -->
              <div class="mt-8 pt-8 border-t border-gray-800">
                <p class="text-xs font-mono text-gray-500 uppercase tracking-wider">
                  Platform approved for clinical research • IRB compliant • FDA registered
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Footer -->
    <footer class="border-t border-gray-800 py-8">
      <div class="container mx-auto px-4">
        <div class="text-center text-sm font-mono text-gray-500">
          <div class="mb-2">© 2024 Pixelated Empathy Research Platform</div>
          <div>All clinical data peer-reviewed • Evidence-based methodology • Research-grade security</div>
        </div>
      </div>
    </footer>
  </div>
</BaseLayout>

<style>
  /* Editorial Typography */
  .editorial-headline {
    font-family: 'Times New Roman', serif;
    letter-spacing: -0.02em;
    line-height: 0.9;
  }

  .editorial-section-header {
    font-family: 'Times New Roman', serif;
    letter-spacing: -0.01em;
  }

  .editorial-card-title {
    font-family: 'Times New Roman', serif;
  }

  /* Stats Animation */
  .editorial-stat {
    animation: editorial-fade-in 0.8s ease-out;
    animation-fill-mode: both;
  }

  .editorial-stat:nth-child(1) { animation-delay: 0.1s; }
  .editorial-stat:nth-child(2) { animation-delay: 0.2s; }
  .editorial-stat:nth-child(3) { animation-delay: 0.3s; }
  .editorial-stat:nth-child(4) { animation-delay: 0.4s; }

  @keyframes editorial-fade-in {
    0% { 
      transform: translateY(20px);
      opacity: 0;
    }
    100% { 
      transform: translateY(0);
      opacity: 1;
    }
  }

  /* Button Effects */
  .editorial-button {
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .editorial-button::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
    transition: left 0.5s ease;
  }

  .editorial-button:hover::before {
    left: 100%;
  }

  .editorial-button-outline {
    position: relative;
    transition: all 0.3s ease;
  }

  .editorial-button-outline:hover {
    box-shadow: inset 0 0 20px rgba(239, 68, 68, 0.1);
  }

  /* Card Effects */
  .editorial-card {
    transition: all 0.3s ease;
    position: relative;
  }

  .editorial-card:hover {
    transform: translateY(-2px);
  }

  .editorial-card::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 2px;
    background: #ef4444;
    transition: width 0.3s ease;
  }

  .editorial-card:hover::after {
    width: 100%;
  }

  /* Testimonial Layout */
  .editorial-testimonial {
    background: rgba(17, 24, 39, 0.5);
    border: 1px solid rgba(55, 65, 81, 0.5);
    padding: 2rem;
    transition: all 0.3s ease;
  }

  .editorial-testimonial:hover {
    border-color: rgba(239, 68, 68, 0.3);
    background: rgba(17, 24, 39, 0.7);
  }

  /* CTA Section */
  .editorial-cta {
    position: relative;
    background: linear-gradient(135deg, rgba(17, 24, 39, 0.8), rgba(31, 41, 55, 0.6));
  }

  .editorial-cta::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: repeating-linear-gradient(
      90deg,
      transparent,
      transparent 2px,
      rgba(239, 68, 68, 0.05) 2px,
      rgba(239, 68, 68, 0.05) 4px
    );
  }

  /* Newspaper Print Simulation */
  @media print {
    .editorial-headline {
      color: #000 !important;
    }
    
    .editorial-button,
    .editorial-button-outline {
      display: none;
    }
  }

  /* Hover Effects for Links */
  a:hover {
    text-decoration: underline;
    text-decoration-color: #ef4444;
  }

  /* Typography Improvements */
  p {
    text-align: justify;
    hyphens: auto;
  }

  /* Responsive Typography */
  @media (max-width: 768px) {
    .editorial-headline {
      font-size: 3rem;
      line-height: 0.95;
    }
    
    .editorial-section-header {
      font-size: 2.5rem;
    }
  }

  /* Loading Animation for Stats */
  .editorial-stat .text-4xl,
  .editorial-stat .text-5xl {
    font-variant-numeric: tabular-nums;
    animation: counter-load 1.5s ease-out;
  }

  @keyframes counter-load {
    0% { 
      transform: scale(0.8);
      opacity: 0;
    }
    50% {
      transform: scale(1.1);
    }
    100% { 
      transform: scale(1);
      opacity: 1;
    }
  }
</style> 