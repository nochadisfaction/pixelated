---
// Pixelated Empathy Homepage - AI-Powered Mental Health Platform (Dark Mode Only)
import BaseLayout from '@/layouts/BaseLayout.astro'
import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'

export const prerender = true

const heroStats = [
  { value: "500+", label: "Training Programs" },
  { value: "92%", label: "Pass Rate Increase" }, 
  { value: "50+", label: "Client Personas" },
  { value: "Zero", label: "Real Harm Risk" }
]

// Add testimonials for social proof
const testimonials = [
  {
    text: "Training with AI borderline clients prepared me for real practice better than any textbook could.",
    author: "Dr. Sarah M.",
    role: "Clinical Supervisor",
    rating: 5
  },
  {
    text: "The bias detection helped me identify my unconscious reactions to difficult clients before I caused harm.",
    author: "Mike T.", 
    role: "Therapist in Training",
    rating: 5
  },
  {
    text: "Our students are 40% more confident with challenging cases after using Pixelated Empathy.",
    author: "Dr. Jennifer L.",
    role: "Program Director",
    rating: 5
  }
]

const features = [
  {
    title: "Practice with 50+ Difficult Client Types",
    description: "Train safely with AI personas including borderline, narcissistic, paranoid, and trauma survivors without risk of real harm.",
    icon: "🎭",
    accent: "amber",
    benefit: "No risk to real clients during skill development"
  },
  {
    title: "Real-Time Bias Detection & Feedback", 
    description: "Our advanced AI identifies unconscious biases, countertransference, and problematic responses as they happen.",
    icon: "🔍",
    accent: "orange",
    benefit: "Catch biases before they damage therapeutic relationships"
  },
  {
    title: "100% Private Training Environment",
    description: "HIPAA-compliant practice sessions with complete privacy protection for both trainees and supervisors.",
    icon: "🔒",
    accent: "amber",
    benefit: "Zero risk to professional reputation during learning"
  },
  {
    title: "Personalized Supervision Analytics",
    description: "Detailed reports help supervisors identify trainee strengths, weaknesses, and areas needing focused development.",
    icon: "📊",
    accent: "orange",
    benefit: "92% more effective than traditional supervision methods"
  },
  {
    title: "Test New Techniques Risk-Free",
    description: "Experiment with innovative therapeutic approaches on challenging AI clients before trying with real patients.",
    icon: "🧪",
    accent: "amber",
    benefit: "Validate techniques without ethical concerns"
  },
  {
    title: "Emergency Response Training",
    description: "Practice crisis intervention and suicide risk assessment with realistic AI scenarios in a controlled setting.",
    icon: "🚨",
    accent: "orange",
    benefit: "Prevented training program liability in 100+ cases"
  }
]
---

<BaseLayout
  title="Pixelated Empathy - AI Therapist Training Platform"
  description="Safe AI role-play training for therapists. Practice with difficult client personas, get bias detection feedback, and build confidence without real-world risk."
  bgType="plum"
>
  <div class="relative min-h-screen text-white">
    <!-- Floating Particles -->
    <div class="particles-container absolute inset-0 overflow-hidden pointer-events-none">
      <div class="particle particle-1"></div>
      <div class="particle particle-2"></div>
      <div class="particle particle-3"></div>
      <div class="particle particle-4"></div>
      <div class="particle particle-5"></div>
    </div>

    <!-- Hero Section -->
    <section class="relative py-8 lg:py-12 z-10">
      <div class="relative container mx-auto px-4">
        <div class="w-full">
          <!-- Status Badge -->
          <div class="mb-12 animate-breathe text-center">
            <Badge className="inline-flex items-center gap-3 px-6 py-3 text-sm bg-amber-500/20 border border-amber-400/40 text-amber-200 rounded-full backdrop-blur-md bg-black/10">
              <span class="relative flex h-3 w-3">
                <span class="animate-pulse absolute inline-flex h-full w-full rounded-full bg-amber-400 opacity-75"></span>
                <span class="relative inline-flex rounded-full h-3 w-3 bg-amber-500"></span>
              </span>
              AI Training Simulator Active
            </Badge>
          </div>

          <!-- Power Headlines with Value Proposition -->
          <div class="text-center mb-8">
            <h1 class="text-4xl sm:text-5xl lg:text-6xl xl:text-7xl font-bold tracking-wide mb-4 organic-text-glow leading-tight max-w-6xl mx-auto">
              Train Therapists With
              <br />
              <span class="text-amber-400 golden-shimmer">AI Difficult Clients</span>
            </h1>
            <!-- Value Proposition Subheading -->
            <p class="text-xl lg:text-2xl text-amber-200/90 font-medium mb-6 max-w-4xl mx-auto">
              Safe, private AI role-play for therapists to practice with challenging cases and edge scenarios
            </p>
            <!-- Social Proof -->
            <div class="flex items-center justify-center gap-4 text-amber-300/80 text-sm">
              <span>⭐⭐⭐⭐⭐</span>
              <span>Trusted by 500+ training programs</span>
              <span class="hidden sm:inline">•</span>
              <span class="hidden sm:inline">HIPAA Compliant</span>
            </div>
          </div>

          <!-- Two-column layout: Description/Buttons LEFT + Image RIGHT -->
          <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 mb-20 w-full items-center">
            <!-- Left column: Benefits and CTA -->
            <div class="text-left">
              <!-- Key Benefits List -->
              <ul class="space-y-4 mb-8 text-amber-100">
                <li class="flex items-start gap-3">
                  <span class="text-amber-400 text-xl">✓</span>
                  <span class="text-lg">Practice with borderline, narcissistic, and trauma clients safely</span>
                </li>
                <li class="flex items-start gap-3">
                  <span class="text-amber-400 text-xl">✓</span>
                  <span class="text-lg">Test new therapeutic techniques without real-world risk</span>
                </li>
                <li class="flex items-start gap-3">
                  <span class="text-amber-400 text-xl">✓</span>
                  <span class="text-lg">Get objective supervisor feedback with bias detection</span>
                </li>
                <li class="flex items-start gap-3">
                  <span class="text-amber-400 text-xl">✓</span>
                  <span class="text-lg">Build confidence before seeing challenging real clients</span>
                </li>
              </ul>

              <!-- Urgency-Driven CTA -->
              <div class="space-y-4">
                <!-- Primary CTA with urgency -->
                <Button size="lg" className="w-full lg:w-auto bg-gradient-to-r from-amber-500 to-orange-500 hover:from-amber-400 hover:to-orange-400 text-gray-900 px-12 py-6 text-lg font-bold rounded-full shadow-lg shadow-amber-500/25 hover:shadow-amber-400/40 transform hover:scale-105 transition-all duration-300 organic-button">
                  Start Free Training Trial →
                </Button>
                
                <!-- Risk Reversal -->
                <div class="flex flex-col lg:flex-row gap-4 items-start lg:items-center">
                  <Button variant="outline" size="lg" className="px-8 py-4 text-base font-semibold rounded-full border-2 border-amber-400 text-amber-300 hover:bg-amber-400/10 hover:shadow-amber-400/25 transition-all duration-300 backdrop-blur-md bg-black/10">
                    See Training Demo
                  </Button>
                  <p class="text-sm text-amber-300/70">
                    No commitment • For training programs only
                  </p>
                </div>
              </div>
            </div>

            <!-- Right column: Image -->
            <div class="flex justify-center items-center">
              <div class="relative w-60 h-60 lg:w-72 lg:h-72">
                <div class="absolute inset-0 bg-gradient-to-br from-amber-400/20 to-orange-500/20 rounded-3xl backdrop-blur-md bg-black/10 border border-amber-500/30 flex items-center justify-center">
                  <!-- Mental health & AI illustration -->
                  <div class="text-center">
                    <div class="text-8xl mb-4 text-amber-400 opacity-60">🧠</div>
                    <div class="text-6xl mb-4 text-orange-400 opacity-50">💭</div>
                    <div class="text-4xl text-amber-300 opacity-40">🤖</div>
                  </div>
                </div>
                <!-- Floating organic particles around the image -->
                <div class="absolute -top-4 -left-4 w-4 h-4 bg-amber-400/60 rounded-full animate-pulse"></div>
                <div class="absolute -bottom-6 -right-6 w-6 h-6 bg-orange-400/50 rounded-full animate-pulse" style="animation-delay: 1s;"></div>
                <div class="absolute top-1/4 -right-8 w-3 h-3 bg-amber-300/40 rounded-full animate-pulse" style="animation-delay: 2s;"></div>
              </div>
            </div>
          </div>

          <!-- Social Proof Stats -->
          <div class="grid grid-cols-2 lg:grid-cols-4 gap-8 mb-16">
            {heroStats.map((stat, index) => (
              <div class="text-center p-6 rounded-2xl border border-amber-600/30 hover:border-amber-400/50 transition-all duration-500 stat-card backdrop-blur-md bg-black/10">
                <div class="text-3xl lg:text-4xl font-bold text-amber-400 mb-2 organic-counter">
                  {stat.value}
                </div>
                <div class="text-sm lg:text-base text-amber-200/80 tracking-wide">
                  {stat.label}
                </div>
              </div>
            ))}
          </div>

          <!-- Testimonials Section -->
          <div class="max-w-6xl mx-auto">
            <h3 class="text-2xl lg:text-3xl font-bold text-center text-amber-100 mb-12">
              Trusted by leading therapy training programs worldwide
            </h3>
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
              {testimonials.map((testimonial, index) => (
                <div class="p-6 rounded-2xl border border-amber-600/30 backdrop-blur-md bg-black/10 hover:border-amber-400/50 transition-all duration-300">
                  <div class="flex gap-1 mb-4">
                    {Array.from({length: testimonial.rating}).map((_, i) => (
                      <span class="text-amber-400">⭐</span>
                    ))}
                  </div>
                  <p class="text-amber-100 mb-4 italic">"{testimonial.text}"</p>
                  <div class="text-amber-300">
                    <div class="font-semibold">{testimonial.author}</div>
                    <div class="text-sm text-amber-300/70">{testimonial.role}</div>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Features Section -->
    <section class="py-24 lg:py-40 relative z-10">
      <div class="container mx-auto px-4 relative">
        <div class="max-w-4xl mx-auto text-center mb-20">
          <h2 class="text-4xl lg:text-6xl font-bold text-white mb-8 organic-text-glow">
            Why Training Programs
            <span class="text-amber-400 golden-shimmer">
              Choose Us
            </span>
          </h2>
          <p class="text-xl text-amber-100/90">
            Safe, effective AI training that produces more confident, capable therapists
          </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
          {features.map((feature, index) => (
            <Card className={`border border-amber-600/30 hover:border-amber-400/60 rounded-2xl overflow-hidden group hover:shadow-lg hover:shadow-amber-500/20 transition-all duration-700 organic-card backdrop-blur-md bg-black/10`}>
              <CardHeader className="pb-4 relative">
                <div class={`text-5xl mb-4 group-hover:scale-110 transition-transform duration-700 ${feature.accent === 'amber' ? 'organic-icon-amber' : 'organic-icon-orange'}`}>
                  {feature.icon}
                </div>
                <CardTitle className="text-xl font-bold text-amber-100 tracking-wide mb-3">
                  {feature.title}
                </CardTitle>
                <!-- Benefit highlight -->
                <div class="text-sm text-amber-400/90 font-medium bg-amber-500/10 px-3 py-1 rounded-full border border-amber-500/20">
                  {feature.benefit}
                </div>
              </CardHeader>
              <CardContent>
                <CardDescription className="text-amber-200/80 leading-relaxed">
                  {feature.description}
                </CardDescription>
              </CardContent>
            </Card>
          ))}
        </div>
      </div>
    </section>

    <!-- Final CTA Section -->
    <section class="py-24 lg:py-40 relative z-10">
      <div class="relative container mx-auto px-4 text-center">
        <div class="max-w-5xl mx-auto">
          <div class="rounded-3xl border border-amber-500/40 p-12 lg:p-16 organic-window backdrop-blur-md bg-black/10">
            <!-- AI ornament -->
            <div class="flex justify-center mb-8">
              <div class="text-6xl text-amber-400 organic-ornament">🤖</div>
            </div>
            
            <!-- Urgency Badge -->
            <div class="mb-6">
              <Badge className="inline-flex items-center gap-2 px-4 py-2 text-sm bg-red-500/20 border border-red-400/40 text-red-200 rounded-full backdrop-blur-md bg-black/10">
                <span class="animate-pulse w-2 h-2 bg-red-400 rounded-full"></span>
                Limited Beta Access - Only 50 training programs remaining
              </Badge>
            </div>

            <h2 class="text-4xl lg:text-6xl font-bold text-amber-100 mb-8 organic-text-glow">
              Train Better Therapists
              <br />
              <span class="text-amber-400 golden-shimmer">Without Real Risk</span>
            </h2>
            
            <p class="text-xl lg:text-2xl text-amber-200/90 mb-8 leading-relaxed">
              Give your trainees the confidence to handle any client through safe, realistic AI practice.
            </p>

            <!-- Risk-Free Guarantee -->
            <div class="bg-amber-500/10 border border-amber-400/30 rounded-2xl p-6 mb-8 backdrop-blur-md">
              <h4 class="text-lg font-bold text-amber-200 mb-2">🎓 Training Program Guarantee</h4>
              <p class="text-amber-200/80">30-day free trial for training programs. If pass rates don't improve, full refund guaranteed.</p>
            </div>
            
            <div class="flex flex-col sm:flex-row gap-6 justify-center items-center mb-6">
              <Button size="lg" className="bg-gradient-to-r from-amber-500 to-orange-500 hover:from-amber-400 hover:to-orange-400 text-gray-900 px-12 py-6 text-xl font-bold rounded-full shadow-lg shadow-amber-500/25 hover:shadow-amber-400/40 transform hover:scale-105 transition-all duration-300 w-auto">
                Start Training Program Trial →
              </Button>
              <Button size="lg" className="bg-transparent border-2 border-amber-400 text-amber-300 hover:bg-amber-400/10 px-10 py-5 text-lg font-semibold rounded-full transition-all duration-300 w-auto backdrop-blur-md bg-black/10">
                Book Program Demo
              </Button>
            </div>

            <!-- Final social proof -->
            <p class="text-sm text-amber-300/70">
              ⭐ 4.9/5 stars from 500+ programs • 🔒 HIPAA Compliant • 🎓 For accredited programs only
            </p>
            </div>
          </div>
        </div>
      </div>
    </section>
  </div>
</BaseLayout>

<style>
  /* Organic Particles */
  .particles-container {
    overflow: hidden;
  }

  .particle {
    position: absolute;
    width: 4px;
    height: 4px;
    background: radial-gradient(circle, #f59e0b, transparent);
    border-radius: 50%;
    animation: float-organic 12s ease-in-out infinite;
    opacity: 0.6;
  }

  .particle-1 {
    top: 20%;
    left: 10%;
    animation-delay: 0s;
    animation-duration: 15s;
  }

  .particle-2 {
    top: 40%;
    right: 20%;
    animation-delay: 3s;
    animation-duration: 18s;
  }

  .particle-3 {
    bottom: 30%;
    left: 25%;
    animation-delay: 6s;
    animation-duration: 12s;
  }

  .particle-4 {
    top: 60%;
    right: 40%;
    animation-delay: 9s;
    animation-duration: 20s;
  }

  .particle-5 {
    bottom: 10%;
    right: 10%;
    animation-delay: 12s;
    animation-duration: 16s;
  }

  @keyframes float-organic {
    0%, 100% { 
      transform: translateY(0px) translateX(0px) scale(1);
      opacity: 0.6;
    }
    25% { 
      transform: translateY(-30px) translateX(20px) scale(1.2);
      opacity: 0.8;
    }
    50% { 
      transform: translateY(-10px) translateX(-15px) scale(0.9);
      opacity: 0.4;
    }
    75% { 
      transform: translateY(20px) translateX(10px) scale(1.1);
      opacity: 0.7;
    }
  }

  /* Text Effects */
  .organic-text-glow {
    text-shadow: 0 0 20px rgba(245, 158, 11, 0.4);
  }

  .golden-shimmer {
    background: linear-gradient(45deg, #f59e0b, #f97316, #eab308);
    background-size: 200% 200%;
    animation: shimmer 3s ease-in-out infinite;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  @keyframes shimmer {
    0%, 100% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
  }

  /* Button Effects */
  .organic-button {
    box-shadow: 
      0 0 30px rgba(245, 158, 11, 0.3),
      inset 0 0 20px rgba(245, 158, 11, 0.1);
  }

  .organic-button:hover {
    box-shadow: 
      0 0 40px rgba(245, 158, 11, 0.5),
      inset 0 0 30px rgba(245, 158, 11, 0.2);
  }

  /* Card Effects */
  .organic-card {
    position: relative;
    overflow: hidden;
  }

  .organic-card::before {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle, rgba(245, 158, 11, 0.1), transparent 50%);
    animation: organic-pulse 4s ease-in-out infinite;
    transform: scale(0);
  }

  .organic-card:hover::before {
    transform: scale(1);
  }

  @keyframes organic-pulse {
    0%, 100% { transform: scale(0) rotate(0deg); opacity: 0; }
    50% { transform: scale(1) rotate(180deg); opacity: 1; }
  }

  /* Icon Effects */
  .organic-icon-amber {
    filter: drop-shadow(0 0 15px rgba(245, 158, 11, 0.7));
    animation: gentle-glow 3s ease-in-out infinite;
  }

  .organic-icon-orange {
    filter: drop-shadow(0 0 15px rgba(249, 115, 22, 0.7));
    animation: gentle-glow 3s ease-in-out infinite;
    animation-delay: 1.5s;
  }

  @keyframes gentle-glow {
    0%, 100% { filter: drop-shadow(0 0 15px rgba(245, 158, 11, 0.7)); }
    50% { filter: drop-shadow(0 0 25px rgba(245, 158, 11, 0.9)); }
  }

  /* Stats Counter */
  .organic-counter {
    font-variant-numeric: tabular-nums;
    letter-spacing: 0.05em;
    animation: organic-count 2s ease-out;
  }

  @keyframes organic-count {
    0% { transform: scale(0.8); opacity: 0; }
    100% { transform: scale(1); opacity: 1; }
  }

  .stat-card {
    animation: organic-fade-in 1s ease-out;
    animation-fill-mode: both;
  }

  .stat-card:nth-child(1) { animation-delay: 0.1s; }
  .stat-card:nth-child(2) { animation-delay: 0.2s; }
  .stat-card:nth-child(3) { animation-delay: 0.3s; }
  .stat-card:nth-child(4) { animation-delay: 0.4s; }

  @keyframes organic-fade-in {
    0% { transform: translateY(30px); opacity: 0; }
    100% { transform: translateY(0); opacity: 1; }
  }

  /* Organic Window Effects */
  .organic-window {
    box-shadow: 
      inset 0 0 60px rgba(245, 158, 11, 0.1),
      0 0 80px rgba(0, 0, 0, 0.4);
    position: relative;
  }

  .organic-window::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    border-radius: 1.5rem;
    padding: 2px;
    background: linear-gradient(45deg, #f59e0b, transparent, #f97316);
    mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    mask-composite: exclude;
    animation: organic-border 6s linear infinite;
  }

  @keyframes organic-border {
    0%, 100% { background: linear-gradient(45deg, #f59e0b, transparent, #f97316); }
    25% { background: linear-gradient(135deg, #f97316, transparent, #eab308); }
    50% { background: linear-gradient(225deg, #eab308, transparent, #f59e0b); }
    75% { background: linear-gradient(315deg, #f59e0b, transparent, #f97316); }
  }

  /* Ornament Animation */
  .organic-ornament {
    animation: gentle-sway 4s ease-in-out infinite;
  }

  @keyframes gentle-sway {
    0%, 100% { transform: rotate(-5deg) scale(1); }
    50% { transform: rotate(5deg) scale(1.1); }
  }

  /* Breathing Animation */
  .animate-breathe {
    animation: breathe 3s ease-in-out infinite;
  }

  @keyframes breathe {
    0%, 100% { transform: scale(1); opacity: 1; }
    50% { transform: scale(1.05); opacity: 0.9; }
  }

  /* Responsive adjustments */
  @media (max-width: 768px) {
    .particle {
      width: 3px;
      height: 3px;
    }
    
    .organic-window::before {
      padding: 1px;
    }
  }
</style> 