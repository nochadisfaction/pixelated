---
import BaseLayout from '../../layouts/BaseLayout.astro'

const title = 'Default Transition | Gradiant'
const description = 'Demonstration of the default page transition'
---

<BaseLayout {title} {description} transitionMode="default">
  <main class="container mx-auto py-12 px-4">
    <a
      href="/transitions-demo"
      class="inline-block mb-6 px-4 py-2 bg-gray-200 dark:bg-gray-800 rounded-md hover:bg-gray-300 dark:hover:bg-gray-700 transition-colors"
    >
      &larr; Back to all transitions
    </a>

    <h1 class="text-4xl font-bold mb-8 text-center">Default Transition</h1>
    <p class="text-lg text-center mb-12 max-w-3xl mx-auto">
      This page demonstrates the default transition, which combines a subtle
      zoom effect with fade in/out animation.
    </p>

    <div class="max-w-5xl mx-auto bg-card rounded-lg shadow-md p-8">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        <div class="space-y-6">
          <h2 class="text-2xl font-bold">How it works</h2>
          <p>
            The default transition creates a smooth transition between pages
            with:
          </p>
          <ul class="list-disc list-inside ml-4 space-y-2">
            <li>Fade out of the old page with slight scale down</li>
            <li>Fade in of the new page with slight scale up</li>
            <li>Smooth timing function for natural feel</li>
          </ul>

          <div
            class="bg-gray-100 dark:bg-gray-800 p-4 rounded-md overflow-x-auto"
          >
            <pre
              class="text-sm"><code>// CSS animations for default transition
{`@keyframes old-animation {
  from { opacity: 1; transform: scale(1); }
  to { opacity: 0; transform: scale(0.96); }
}

@keyframes new-animation {
  from { opacity: 0; transform: scale(1.04); }
  to { opacity: 1; transform: scale(1); }
}`}</code></pre>
          </div>
        </div>

        <div>
          <div
            class="aspect-video bg-gray-200 dark:bg-gray-800 rounded-lg flex items-center justify-center mb-6"
          >
            <div class="text-center space-y-4">
              <div class="inline-block p-4 bg-primary text-white rounded-full">
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  width="24"
                  height="24"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                >
                  <path
                    d="M12 8c-2.8 0-5 2.2-5 5s2.2 5 5 5 5-2.2 5-5-2.2-5-5-5zm0 8c-1.7 0-3-1.3-3-3s1.3-3 3-3 3 1.3 3 3-1.3 3-3 3z"
                  ></path>
                  <path
                    d="M3 8c0-3.3 2.7-6 6-6M15 2c3.3 0 6 2.7 6 6M21 16c0 3.3-2.7 6-6 6M9 22c-3.3 0-6-2.7-6-6"
                  ></path>
                </svg>
              </div>
              <p class="font-medium">Default Transition</p>
            </div>
          </div>

          <div class="grid grid-cols-2 gap-4">
            {
              ['fade', 'slide', 'slide-up', 'slide-down', 'zoom'].map(
                (mode) => (
                  <a
                    href={`/transitions-demo/${mode}`}
                    class="block p-4 bg-gray-100 dark:bg-gray-800 rounded-lg hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors text-center"
                  >
                    Try{' '}
                    {mode
                      .split('-')
                      .map(
                        (word) => word.charAt(0).toUpperCase() + word.slice(1),
                      )
                      .join(' ')}
                  </a>
                ),
              )
            }
          </div>
        </div>
      </div>
    </div>
  </main>
</BaseLayout>
