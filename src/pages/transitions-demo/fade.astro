---
import BaseLayout from '@/layouts/BaseLayout.astro'
import { ClientRouter } from 'astro:transitions'
import Link from '@/components/base/Link.astro'

// Sample images for transition demonstration
const images = [
  {
    id: 1,
    src: 'https://images.unsplash.com/photo-1674574124792-3be232f1a2c5?w=800&auto=format&fit=crop',
    alt: 'Colorful abstract gradient',
  },
  {
    id: 2,
    src: 'https://images.unsplash.com/photo-1671226366569-2b8a0aa58bf1?w=800&auto=format&fit=crop',
    alt: 'Vibrant waves pattern',
  },
  {
    id: 3,
    src: 'https://images.unsplash.com/photo-1671226366557-912958a5fef1?w=800&auto=format&fit=crop',
    alt: 'Digital art pattern',
  },
]
---

<BaseLayout
  title="Fade Transition Demo"
  description="Experience the smooth fade transition between page navigations"
  bgType="particle"
>
  <ClientRouter />

  <div class="max-w-3xl mx-auto px-4 py-12" transition:animate="fade">
    <header class="mb-8 flex justify-between items-center">
      <Link href="/transitions-demo" class="text-sm op-70 hover:op-100">
        &larr; Back to Demos
      </Link>

      <h1 class="text-3xl font-bold text-center">Fade Transition</h1>

      <div class="w-20"></div>
      <!-- Spacer for alignment -->
    </header>

    <p class="text-lg mb-8 text-center opacity-80">
      This page demonstrates the fade transition effect. Notice how the content
      smoothly fades in and out during navigation. This creates a gentle, subtle
      transition between pages.
    </p>

    <div class="grid grid-cols-1 gap-8 mb-12">
      {
        images.map((image) => (
          <div class="overflow-hidden rounded-lg shadow-lg">
            <img
              src={image.src}
              alt={image.alt}
              transition:name={`fade-image-${image.id}`}
              class="w-full h-auto object-cover aspect-video"
            />
            <div class="p-4 bg-[#88888811]">
              <h3
                class="text-xl font-medium"
                transition:name={`fade-title-${image.id}`}
              >
                Image {image.id}
              </h3>
              <p class="opacity-70 mt-2">
                This image has a unique transition name for smooth transitions
                between pages.
              </p>
            </div>
          </div>
        ))
      }
    </div>

    <div class="mt-8 p-6 border border-dashed rounded-lg">
      <h2 class="text-2xl font-semibold mb-4">How To Use Fade Transitions</h2>
      <div class="prose max-w-none opacity-80">
        <p>To apply a fade transition to your pages:</p>

        <pre
          class="bg-slate-800 text-slate-200 rounded-md p-4 mt-4 overflow-x-auto">
<code>&lt;main transition:animate="fade"&gt;
  &lt;!-- Your page content here --&gt;
&lt;/main&gt;</code>
        </pre>

        <p class="mt-4">
          You can also customize fade transitions with additional options:
        </p>

        <pre
          class="bg-slate-800 text-slate-200 rounded-md p-4 mt-4 overflow-x-auto">
<code>&lt;main transition:animate={{ name: 'fade', duration: 500 }}&gt;
  &lt;!-- Your page content here --&gt;
&lt;/main&gt;</code>
        </pre>
      </div>
    </div>
  </div>
</BaseLayout>
